{
  "name": "truffle-project",
  "version": "0.1.0",
  "description": "Truffle project with Monopoly contract",
  "scripts": {
    "test": "truffle test",
    "compile": "truffle compile",
    "migrate": "truffle migrate",
    "migrate:dev": "truffle migrate --network development "
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.2",
    "@truffle/hdwallet-provider": "^2.1.7",
    "hardhat": "^2.13.0"
  }
}
